package org.firstinspires.ftc.teamcode.PowerPlay.Auto;

public class RobotMovement {
    public static void goToPosition(double x, double y, double movementSpeed) {

       /* double targetDistance = Math.hypot(x - robotXPosition, y - robotYPosition); // use pythagareon thereom to

        double absoluteAngleToTarget = Math.atan2(y - robotYPosition, x - robotXPosition);

        double relativeAngle = optimizeRelativeAngle(absoluteAngleToTarget - (robotAngle_rad));
*/
    }
}
